<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <title>Editar tema #<%= topic.id %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-50 text-slate-900">
  <main class="max-w-xl mx-auto px-4 py-8">
    <h1 class="text-2xl font-bold mb-4">Editar tema #<%= topic.id %></h1>
    <form action="/topics/<%= topic.id %>?_method=PUT" method="post" class="space-y-3 bg-white border border-slate-200 rounded-xl shadow-sm p-4">
      <label class="block">
        <span class="text-sm text-slate-700">Título</span>
        <input name="titulo" required value="<%= topic.titulo %>"
               class="mt-1 w-full rounded-lg border-slate-300 focus:border-slate-400 focus:ring-slate-200"/>
      </label>

      <label class="block">
        <span class="text-sm text-slate-700">Descripción</span>
        <input name="descripcion" value="<%= topic.descripcion %>"
               class="mt-1 w-full rounded-lg border-slate-300 focus:border-slate-400 focus:ring-slate-200"/>
      </label>

      <label class="block">
        <span class="text-sm text-slate-700">Autor</span>
        <input name="autor" value="<%= topic.autor %>"
               class="mt-1 w-full rounded-lg border-slate-300 focus:border-slate-400 focus:ring-slate-200"/>
      </label>

      <div class="flex gap-2 justify-end pt-2">
        <a href="/" class="rounded-lg border px-4 py-2 hover:bg-slate-50">Cancelar</a>
        <button class="rounded-lg bg-slate-900 text-white px-4 py-2 hover:bg-slate-800">Guardar</button>
      </div>
    </form>
  </main>
</body>
</html>